import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.market.grocery.template',
  appName: 'Grocery UI',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
